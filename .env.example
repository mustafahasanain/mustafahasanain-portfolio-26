# Environment variable template.
#
# Copy this file to `.env.local` for local development. Real `.env*` files are
# git-ignored and must never be committed.
#
# No environment variable is required yet. Add every new variable here with a
# placeholder value (never a real secret) as the phase that introduces it lands.
